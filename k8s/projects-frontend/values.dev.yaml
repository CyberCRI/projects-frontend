injectedEnv:
  nonSensitive:
    VITE_APP_KEYCLOAK_URL: https://lpi-dev.cloud-iam.com/auth
    VITE_APP_VERSION: '{{ .Values.application.revision }}'

domain: k8s.lp-i.dev

certManagerClusterIssuer: letsencrypt-prod
